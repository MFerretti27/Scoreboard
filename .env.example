# Scoreboard App - Environment Variables (Documentation / Future Support)
# NOTE: The app auto-loads this file via python-dotenv if present.
# Do not commit a real .env file containing secrets.

# --- Email Notifications ---
# Enable/disable sending diagnostic emails
EMAIL_ENABLED=true
# Tip: disable emails in development by setting this to false
# EMAIL_ENABLED=false

# SMTP connection details
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587

# Sender account and password/app password
SMTP_USER=your_email@example.com
SMTP_PASSWORD=your_smtp_or_app_password

# Comma-separated list of recipients
TO_EMAILS=recipient1@example.com,recipient2@example.com

# --- Logging (optional) ---
# LOG_LEVEL can be DEBUG, INFO, WARNING, ERROR (optional)
# LOG_LEVEL=INFO
